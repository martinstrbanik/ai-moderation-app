# ----- Benchmark data (large files, not committed to the repository) -----
data/raw/
data/processed/
# but keep the dataset documentation
!data/README.md

# ----- Java / Maven -----
target/
*.class
*.jar
!.mvn/wrapper/maven-wrapper.jar

# ----- Node / Vue -----
node_modules/
dist/
*.log
.vite/

# ----- IDE / OS -----
.idea/
*.iml
.vscode/
.DS_Store

# ----- Keys and secrets -----
.env
*.pem
# local real values (profiles) - not committed; application.properties with placeholders is committed
backend/src/main/resources/application-local.properties