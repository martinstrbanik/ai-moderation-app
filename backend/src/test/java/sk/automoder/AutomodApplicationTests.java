package sk.automoder;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class AutomodApplicationTests {

    @Test
    void contextLoads() {
        // Verifies that the whole application context (including DB and the initializer) loads.
    }
}